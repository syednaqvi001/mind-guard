-- Create database if not exists
CREATE DATABASE IF NOT EXISTS mindguard;

-- Use the database
\c mindguard

-- Create tables from init.sql schema
-- (The full schema will be applied from the original init.sql file)
